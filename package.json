{
  "name": "soapbox",
  "version": "1.0.0",
  "description": "Geopolitical Intelligence OS",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up --build",
    "dev:down": "docker compose -f docker-compose.dev.yml down",
    "dev:logs": "docker compose -f docker-compose.dev.yml logs -f",
    "setup:env": "cp .env.example .env 2>/dev/null || true && cp backend/.env.example backend/.env 2>/dev/null || true && cp frontend/.env.example frontend/.env.local 2>/dev/null || true && echo 'Environment files created! Please review and edit them.'"
  },
  "keywords": [
    "geopolitical",
    "intelligence",
    "dashboard"
  ],
  "author": "",
  "license": "ISC"
}

