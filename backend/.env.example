# Database Configuration
DATABASE_URL="postgresql://postgres:secret@localhost:5432/soapbox?schema=public"

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=dev-jwt-secret-change-in-production

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:3000

# Email Configuration (Mailpit for development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@soapbox.local

# Production Email Configuration (commented out for dev)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourdomain.com
